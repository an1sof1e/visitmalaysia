
class Location {
  String pid, locName, state, description, latitude, longitude, url, contact, address, imagename;
  Location({
    this.pid,
    this.locName,
    this.state,
    this.description,
    this.latitude,
    this.longitude,
    this.url,
    this.contact,
    this.address,
    this.imagename,
 } );

}